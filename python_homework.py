
# # Функция для вычисления суммы всех делителей числа
# def сумма_делителей(n):
#     сумма = 0
#     for i in range(1, n):
#         if n % i == 0:
#             сумма += i
#     return сумма

# # Функция для нахождения всех дружественных чисел в заданном диапазоне
# def дружественные_числа(k):
#     дружественные = []
#     for n in range(1, k + 1):
#         m = сумма_делителей(n)
#         if m > n:  # Проверяем, что сумма делителей больше числа n
#             if сумма_делителей(m) == n:  # Проверяем условие дружественных чисел
#                 дружественные.append((n, m))
#     return дружественные

# # Получаем входные данные от пользователя
# k = int(input("Введите число k: "))

# # Находим все дружественные числа, включая 80 и 106
# результат = дружественные_числа(k)

# # Выводим результат
# print("Пары дружественных чисел в диапазоне от 1 до", k, ":")
# for пара in результат:
#     print(пара[0], пара[1])
    
    
# Функция для вычисления суммы всех делителей числа
def сумма_делителей(n):
    сумма = 0
    for i in range(1, n):
        if n % i == 0:
            сумма += i
    return сумма

# Функция для нахождения всех дружественных чисел, где первое число не превышает введенное пользователем число
def дружественные_числа(k):
    дружественные = []
    for n in range(1, k + 1):
        m = сумма_делителей(n)
        if m > n:  # Проверяем, что сумма делителей больше числа n
            if сумма_делителей(m) == n and m <= k:  # Проверяем условие дружественных чисел и что второе число не превышает k
                дружественные.append((n, m))
    return дружественные

# Получаем входные данные от пользователя
k = int(input("Введите число k: "))

# Находим все дружественные числа, где первое число не превышает k
результат = дружественные_числа(k)

# Выводим результат
print("Пары дружественных чисел, где первое число не превышает", k, ":")
for пара in результат:
    print(пара[0], пара[1])
